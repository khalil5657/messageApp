<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="./styles.css">
</head>
<body>
    <%-include("navbar") %>
    <div class="title"><h1 >Messages</h1></div>
    <% for (let i =0;i<messages.length; i++) {%>
        <div class="message">
    <h4 class="user"><%= messages[i].user%></h4>
    <em><h4 class="text"><%= messages[i].text%></h4></em>
    <span class="date"><%= messages[i].added%></span>
    <a href="./post/<%= i %>">open</a></div>
    <% } %>
</body>
</html>